import React from 'react';
import { AppointmentListView } from '@/components/RoomRegistration/AppointmentListView';

export default function TestRegistrationListPage() {
  return (
    <div className="h-[calc(100vh-2rem)] w-full"> 
      <AppointmentListView />
    </div>
  );
} 